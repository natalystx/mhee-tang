import DashboardScreenComponent from "@/features/dashboard";
import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const Dashboard = () => {
  return (
    <SafeAreaView>
      <DashboardScreenComponent />
    </SafeAreaView>
  );
};

export default Dashboard;
